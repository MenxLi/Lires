// Deprecated
export interface TagCheckStatus{
    identifier: string,
    isChecked: boolean,
    currentlySelected: string[]
}

export interface SearchStatus{
    // dtype: string,
    content: string
}