
export const DOMAINURL = "http://" + window.location.hostname;
export const PORT = window.location.port;

export const BACKENDURL: string = `${DOMAINURL}:${PORT}`;
export const FRONTENDURL: string = `${DOMAINURL}:${PORT}/frontend`;
